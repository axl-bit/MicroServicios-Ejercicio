spring:
  application:
    #indicamos el nombre de la aplicacion
    name: gateway-servicio
  cloud:
    config:
      enabled: true
      #indicamos el puerto del config-server
      uri: http://localhost:8081
